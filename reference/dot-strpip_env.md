# Internal Package Environment

This environment is used to store package-level data that needs to
persist between function calls without polluting the user's global
environment.

## Usage

``` r
.strpip_env
```

## Format

An object of class `environment` of length 0.
