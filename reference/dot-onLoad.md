# Initialize package when loaded

This function is called when the package is loaded via library() or
require()

## Usage

``` r
.onLoad(libname, pkgname)
```

## Arguments

- libname:

  The library where the package is installed

- pkgname:

  The name of the package
